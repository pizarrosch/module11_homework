const result = (x, n) => console.log(x ** n);

result(7, 5);


// как альтернативу можно использовать следующий вариант

const anotherResult = (x, n) => console.log(Math.pow(x, n));
anotherResult(7, 5);